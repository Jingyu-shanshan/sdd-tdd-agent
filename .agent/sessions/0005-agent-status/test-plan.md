# Test plan

1. Happy path: load all fields from platform-generated YAML and session JSON.
2. CLI boundary: assert exact line ordering, values, newline, and exit code.
3. Boundary: initialized project with no classification or active session.
4. Regression: run all existing CLI, detection, and initialization tests.

