# Acceptance criteria

- [x] JSON and Codex protocols select matching adapters.
- [x] Missing active Session/configuration fails before process use.
- [x] Valid output writes the plan and enters IMPLEMENTATION.
- [x] `agent tests` output and failures are deterministic.
- [x] Existing validation remains authoritative.
- [x] No dependency or `.gitignore` update is needed.
