# Design

Use the existing package version and GitHub-native pull request, tag, Release,
and artifact features. Documentation is the only tracked source change; no new
release abstraction, dependency, runtime command, or workflow is required.

The existing Linux Mint workflow remains untouched until a labelled real host
exists. Release notes must state that boundary rather than converting Ubuntu
compatibility evidence into a Mint claim.
