# Test Plan

- Assert the exact Java and TypeScript capability records.
- Assert deterministic output and ordering.
- Assert CLI listing creates no workspace and executes no toolchain.
- Retain all focused detectors, planners, Angular contracts, and hosted real
  fixture coverage.
