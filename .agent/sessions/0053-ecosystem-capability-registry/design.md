# Design

Use an immutable dataclass tuple containing only the two documented target
languages and their verified tools/frameworks. Render deterministic text through
a side-effect-free CLI branch. This is a discovery surface, not a second
detector; production project detection and command planners remain authoritative
for accepting an actual workspace.
