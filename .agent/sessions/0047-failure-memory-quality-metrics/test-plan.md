# Test Plan

- Model/test exceptions and nonzero exits create no-content fingerprints.
- Repeated signatures across Sessions merge counts and bounded Session IDs.
- Invalid/tampered/symlinked/colliding memory files fail safely.
- Planned/completed task and test prefixes produce exact rates.
- Test/refactor success and mean duration derive from validated events.
- Cost remains unavailable unless all events contain verified usage.
- Both CLI views are deterministic and active-project scoped.
