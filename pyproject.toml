[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sdd-tdd-agent"
version = "0.1.0"
description = "A single-agent SDD and incremental TDD development CLI"
requires-python = ">=3.9"

[project.scripts]
agent = "sdd_tdd_agent.cli:main"
